{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=eplfantasy;Username=eplfantasy_app;Password=dev-only-not-a-real-secret"
  },
  "Jwt": {
    "Issuer": "eplfantasy-dev",
    "Audience": "eplfantasy-dev",
    "_SigningKeyNote": "Local development only, per BR-173 — production keys are injected via environment/secret-store, never committed here.",
    "SigningKey": "dev-only-signing-key-not-for-production-use-replace-me-32bytes+"
  }
}
